export enum AppRoute {
  Main = '/test-task-gna',
}

export enum NameSpace {
  Data = 'DATA',
}

export enum APIRoute {
  Products1 = '/documents1',
  Products2 = '/documents2',
  Cancel = '/cancel',
}
